pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh 'echo "mvn clean install"'
		sh 'echo "mvn clean test, component test"' 
		sh 'echo "mvn deploy"' 
      }


    }
  }
}
